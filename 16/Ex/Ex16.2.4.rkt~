;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-intermediate-reader.ss" "lang")((modname Ex16.2.5) (read-case-sensitive #t) (teachpacks ((lib "draw.rkt" "teachpack" "htdp"))) (htdp-settings #(#t constructor repeating-decimal #f #t none #f ((lib "draw.rkt" "teachpack" "htdp")))))
(define-struct dir (name content))
(make-dir n c)
#|
A list-of-files-and-directories is either:
1.empty
2.(cons f d1) where f is file and d1 is LOFD or
3.(cons d1 d2) where d1 is dir and d2 is LOFD.
|#

